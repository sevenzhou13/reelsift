# Reelwave portfolio assets

The page intentionally does not fabricate product screenshots or demo footage. Add the reviewed real assets below before public sharing:

- `hero-story-canvas.png` — real Story Canvas overview, ideally 1600 px wide or larger.
- `finder-open-reelwave.png` — Finder context menu showing “Open in Reelwave”.
- `codex-story-test.png` — real Codex substitution-test capture with private paths and personal data removed.
- `story-before.png` — cooking clip in `03 · 下班后的生活`.
- `story-after.png` — cooking clip in `01 · 刚到北京`, with Story Change visible.
- `drag-story-change.mp4` — 8–12 second muted loop of the drag → suggestion → Apply flow.
- `reelwave-demo-40s.mp4` — hero demo using the script in `Reelwave_Portfolio_Spec.md`.
- `reelwave-demo-90s.mp4` — full case demo using the script in `Reelwave_Portfolio_Spec.md`.

Before adding any capture, remove API keys, account data, absolute local paths, private footage that cannot be published, and Finder sidebar details that identify the machine or owner.
